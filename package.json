{
  "name": "times-up",
  "private": true,
  "version": "1.0.0",
  "description": "multi-timer sequencer for keeping track of time data for multiple sequential tasks.",
  "repository": "https://github.com/johnedisc/times_up.git",
  "author": "johnedisc",
  "license": "MIT",
  "scripts": {
    "startDev": "nodemon server/dist/server.js",
    "start": "node server/dist/server.js",
    "build:client": "tsc -p client/",
    "build:server": "tsc -p server/",
    "build": "yarn build:client && yarn build:server",
    "dev": "yarn build:client && yarn build:server"
  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "typescript": "^5.0.2"
  }
}
