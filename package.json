{
  "name": "times-up",
  "private": true,
  "version": "1.0.0",
  "description": "multi-timer sequencer for keeping track of time data for multiple sequential tasks.",
  "repository": "https://github.com/johnedisc/times_up.git",
  "author": "johnedisc",
  "license": "MIT",
  "scripts": {
    "startDev": "nodemon server/dist/server.js",
    "start": "node server/dist/server.js",
    "build:clientDev": "cd client/ && npx tsc -w",
    "build:serverDev": "cd server/ && npx tsc -w",
    "build": "npx tsc -p server/ && npx tsc -p client/"
  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "nodemon": "^3.0.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/dotenv": "^8.2.0",
    "@types/jsonwebtoken": "^9.0.3",
    "@types/pg": "^8.10.2",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3"
  }
}
